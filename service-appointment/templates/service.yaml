apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-appointment.fullname" . }}
  labels:
    {{- include "service-appointment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-appointment.selectorLabels" . | nindent 4 }}
