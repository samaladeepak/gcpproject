apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-order.fullname" . }}
  labels:
    {{- include "service-order.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-order.selectorLabels" . | nindent 4 }}
